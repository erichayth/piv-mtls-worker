{
  "name": "piv-mtls-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for mTLS Certificate Validation",
  "main": "piv-mtls-worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "mtls",
    "piv",
    "certificate"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}